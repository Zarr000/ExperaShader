# Photorealistic shader pack (Iris/OptiFine)
# Original implementation focused on physically based rendering and modern post-processing.

shaderpack=photorealistic-aaa
name=Photorealistic AAA

# Compatibility
# Supports Iris and OptiFine shader pipelines.
version=1.0

# Rendering pipeline
# GBuffer + deferred lighting

# Pass list is consumed by Iris/OptiFine depending on the pipeline.
# We provide a full set of passes.

# Core passes
gbuffers=1

# Optional passes
# Order is controlled by shaderpack loader.


# Include libraries are referenced by shaders using #include

# Quality presets
# Users can select in-game if supported by the loader.

